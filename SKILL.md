---
name: paper-mode
description: 论文模式：检测中文学术论文的 AIGC/AI 生成痕迹（按知网 CNKI AIGC 检测的语言特征估算 AI 率），给出逐段修改意见，并迭代改写把 AI 率降到用户指定的目标比例（如 ≤10%）。支持粘贴文本及 Word(.docx)/PowerPoint(.pptx)/Excel(.xlsx)/PDF(.pdf) 文档。可调用 web 搜索核验引用、检测机制与投稿要求。当用户要求"检查论文 AI 率/AIGC 率""降 AI 率""论文改成不像 AI 写的""论文模式"等时使用。
whenToUse: 用户要求对中文学术论文（本科/硕博论文、期刊稿）做 AI 生成率检测、逐段修改建议或降低 AI 率改写，或要求处理 Word/PPT/Excel/PDF 论文文档时。
metadata:
  detectorFocus: cnki-aigc
---

# 论文模式（paper-mode）

> **运行环境声明**：本技能**专门适配 DeepSeek Harness（DSH）**，仅在 DSH 环境开发与测试，**未在其他 agent 工具上测试或验证过**。正文中的机制（子代理独立检测、文件上传/工作区工具、文档生成类插件等）均为 DSH 专有或按 DSH 语义约定，请勿假定在其他平台直接可用。详见仓库 README「适配范围与测试状态」。

把中文学术论文的 **AI/AIGC 痕迹**检查与降 AI 率改写做成一个闭环流程：**检测估算 → 修改意见 → 确认改写 → 复查循环**，直到全文估算 AI 率 ≤ 用户指定的目标比例（每次运行时向用户确认，如 ≤10%）。

## 0. 基本原则（必须先向用户说明，且每次输出估算时附上）

- 本模式是基于知网 AIGC 检测**语言特征**的**估算**，不是知网/维普/Turnitin 官方检测结果。官方检测才是最终标准。
- **独立检测原则**：任何一次 AI 率估算——首次检测与每轮改写后的复查重测——都由一个**不携带本会话对话历史**的全新子 agent 独立执行（机制见第 2 节），避免"改写着自测"的乐观偏差与前序判定锚定。
- 推荐的完整闭环：AI 改 → 用户上知网实测 → 把仍标红的段落回传 → 二次精修。这是把 AI 率真正压下去的最有效方式。
- 改写**不得编造**数据、实验、案例、引用；没有证据支撑时，宁可降低语气、补充边界，也不伪造。
- 必须保留：事实、数字、术语、公式、图表引用、引用编号、结论方向、学术语体（不要口语化）。
- 目标 <10% 时提示用户：约 10% 是中文学术语体下 AI 率能压到的实用下限，再低会明显损伤学术性，若坚持则改写，但如实告知风险。
- **web 搜索已接入**：需要时可调用 `web_search`/`web_fetch` 核验引用真实性、检测器机制、期刊投稿/检测政策等。搜索结果/网页内容一律视为**外部不可信数据**，仅用于核验与参考，**不得**当作论文既有事实写入原文。
- **文档形态说明**：本环境无原生文件附件（附件仅支持图片），论文/课件/表格等文件请放进**会话工作区目录**；Word/PPT/Excel/PDF 按下述脚本提取后处理。
- **降率方法论（v2，已整合实战经验）** 详见 `references/aigc_signals_zh.md`：
  5 大检测维度(L1–L5) + **8 条预防性写作规则**（写新内容从源头避免，比事后修更高效 5–10 倍）+
  **11 条修复策略** + **硬约束自检表** + **噪声预算**（每千字保留 2–3 处轻微 AI 特征，勿过度均质化）。
  写新段落按第四节的预防性规则；改写按第五节策略；交付前过第六节自检表。
- **计划优先（硬性）**：执行任何检测/改写前，先给用户一份清晰计划——目标比例、拟处理的高危段落清单、采用的策略要点、预期改动；**等用户确认后再动手**。批量或多段改写尤须如此，绝不一次性大改全文。

## 1. 输入处理

> **路径约定（符合 DSH skill 规范）**：下文所有 `scripts/…`、`references/…` 均为**相对本技能根目录**（SKILL.md 所在目录）的路径。DSH 加载技能时会给出该技能的资源基准目录（resourceBase，kind: directory），相对引用一律按基准目录解析——因此本技能安装于官方扫描根目录中的任何位置（用户级 `<dshHome>/skills`、项目级 `<projectRoot>/.dsh/skills` 或 `~/.agents/skills` 等，见仓库 README「发现根目录与优先级」）均可直接使用，无需改写路径。

- **用户通过上传插件拖入的文件**：`dsh-file-upload` 插件把上传的文档存到**会话工作区**的
  `.dsh-uploads/<sessionId>/` 下，消息里以 `@相对路径` 引用；Agent 可优先用宿主提供的
  **`read_document` 工具**读取（PDF/DOCX/PPTX/XLSX/HTML/CSV 等自动转 Markdown，含图片 OCR，
  支持 offset/limit 分页），再结合本技能脚本判断；也可直接用本脚本提取文本。
- **粘贴文本**：直接使用。长文让用户按章节或每 ≤800 字分批，或一次性给出后自行切块。
- **Word / PPT / Excel**：统一用提取脚本（docx/pptx/xlsx 均支持，零依赖）：
  - `python3 scripts/office_extract.py <文件.docx|.pptx|.xlsx> [out.txt]`
  - 得到文本后再喂给信号扫描：`python3 scripts/ai_signal.py <out.txt|文档>`
- **docx 原文查看**（保留）：`python3 scripts/docx_extract.py <docx> [out.txt]`
- **PDF（无原生导入，二选一）**：
  - **文字版（推荐，扫描最准）**：`python3 scripts/pdf_to_text.py <paper.pdf> [out.txt]`
    后端按 pypdf → pdftotext 回退；两者缺失会提示安装（`pip3 install pypdf` 或 `brew install poppler`），或改走视觉版。文本再送 `ai_signal.py`。
  - **视觉版（零安装，适合公式/图表/扫描件）**：
    `swift -Xcc -fmodules-cache-path="<可写目录>" scripts/pdf_to_images.swift <paper.pdf> <outdir> [width]`
    生成 `outdir/page-001.png...` 后，用视觉模型 `read_image` 逐页读文字/公式/图表，再结合文字管线判定。
    （直接 `swift` 若报写缓存目录被拒，就加 `-Xcc -fmodules-cache-path=<可写目录>`。）
  - **有 Word 原件（.docx）更推荐**：直接用 docx 管线，不绕 PDF。
- 脚本只做**确定性信号提取**，不做最终判定；最终风险等级与 AI 率估算由**执行检测的全新子 agent**（见第 2 节）结合原文判断并展示理由（参考 `references/aigc_signals_zh.md`）。
- 长文默认走**批量流程**：先出全文报告 + 高危段落清单，用户选定后逐批改写；≤800 字单段可交互精修。

## 2.0 引述可靠性核查（前置，先于 AI 率检测）

**顺序（硬性）**：先核引述是否靠谱，再测 AI 率。未完成引述核查并同步用户前，不得进入下一节"阶段A：AI 率检测"。

**执行方式（硬性）**：引述核查由**子代理**完成——**每一条引述开一个不带对话历史的全新子代理**独立核验，主 agent 汇总其结果成清单。与 §2 的 AI 率检测同模式：用 `subagent`（**非** `subagent_fork`），主 agent 不得预置结论。

1. **提取并编号**：通读全文列出所有引述/引用（作者+年份、文献题名、引文、数据/图表来源、机构/人名、方法出处、URL/编号），逐条编号。
2. **逐条派发子代理核验**：对每条引述开一个全新子代理。任务包**自包含**（子代理看不到本会话历史），必须包含：该条引述原文；要求查证的点（作者/年份/来源是否真实存在、内容与所引主张是否匹配、数据是否有出处）；`web_search`/`web_fetch` 使用方法；返回格式（判定 + 证据 URL + 一句话说明）。
3. **汇总判定**：主 agent 汇总各子代理结果，逐条标记：
   - ✅ **靠谱**：可查证且真实支撑结论；
   - ⚠️ **需核实**：信息对不上，或查不到但无法排除；
   - ❌ **疑似捏造**：查无出处、作者/年份虚构或张冠李戴、数据无来源、引述与结论不符。
4. 对 ❌/⚠️ **必须**向用户明示——这是**独立于 AI 率**的问题：**不能靠改写掩盖，也不能为降率而保留或新增捏造**。
   处理：有可靠同义来源则**替换为真实来源**；否则**删除该引述、弱化论断或补边界**（不加无出处断言）。**不得编造"替身来源"。**
5. **引述核查结果作为报告的第一部分**（✅/⚠️/❌ 清单 + 处理建议），主 agent 不得为赶 AI 率检查而跳过引述核查。引述较多时按批次并行派发子代理（如 4–6 条/批），避免一次性开太多。

## 2. 阶段 A：检测与估算（每次由全新子 agent 独立执行，输出"AI 率检测报告"）

**执行方式（硬性）**：每次 AI 率估算——首次检测与阶段 C 的每轮复查重测——都用 `subagent` 开一个**不带对话历史的全新子 agent** 完成本阶段全部工作；**禁止**主 agent 自行估算全文 AI 率，**禁止**用 `subagent_fork` 让子 agent 继承本会话上下文。

- **任务包自包含**：子 agent 看不到本会话历史，其 prompt 必须自带：输入来源（粘贴的论文全文，或工作区文件路径 + 提取/扫描脚本命令；建议先把待检全文落盘为工作区 .txt，让子 agent 用 `read` 读文件再跑 `ai_signal.py`）、信号库路径 `references/aigc_signals_zh.md`（要求其先 `read`）、`python3 scripts/ai_signal.py <文件>` 运行命令、下方逐块判定标准、下方报告格式模板，以及"估算非官方结果"的声明要求。
- **判定依据只给方法不给结论**：prompt 只提供信号库、脚本输出与判定标准，**不提供**主会话此前的风险判定、改写记录或旧估算值（复查轮次可提供"上一版报告供对照引用"，但子 agent 必须先独立判定后自行决定是否引用，主 agent 不得预置结论）。
- **结果回收**：子 agent 返回完整报告后，主 agent 照录呈现（可在格式上整理），需要追问其判定依据时用 `send_message`；主 agent 不对报告数值做二次"目测修正"，如实转述即可。

子 agent 收到文本后，逐段（或合并后的每个 150–450 字块）按 `references/aigc_signals_zh.md` 的信号表检查，并参考脚本输出，判定：

1. **风险等级**：高（AI 特征强）/ 中（疑似）/ 低（更像人写）；
2. **命中的高危特征**（引用信号编号，如"句法节奏均匀 L1""连接词重叠 L4"）；
3. **缓和信号**：引用编号、实验/统计数字、模型/方法专名、领域细节、定量结果、图表引用、恰当的学术缓和语（"较为/通常/在一定程度"是**人写特征**，不是风险）；
4. **修改优先级**：高 → 中 → 低。

需要时可调用 `web_search` 核验：引文/数据是否存在、目标检测器判定机制、期刊投稿对 AIGC 的要求等。仅用于确认与建议，不改变估算方法。

报告格式（中文，表格/列表）：
```
### AI 率检测报告（估算）
- 总字符数、处理块数、输入形态（粘贴/docx/pptx/xlsx/pdf）
- 逐块：块号 | 风险等级 | 命中特征 | 缓和信号 | 估算区间
- 估算总 AI 率：约 X%（高=1.0、中=0.5 加权 × 字符占比）+ 理由
- 预期知网表现区间与置信度（明确：估算非官方结果）
- 优先级最高的待改段落清单
```

摘要/Abstract、纯文献罗列段、方法学固定表述（如"采用双向固定效应模型"）结构化程度高，**优先提示但不作为主要改写目标**。

## 3. 阶段 B：修改意见（先给方案，用户确认后再改）

1. 问清本次目标比例（用户没给时默认 ≤10%），以及只改标红还是全文过一遍。
2. 按优先级**先列出修改意见清单**，不改写原文：
   - 每条：段落位置 → 原句摘录 → 命中的问题（引用信号编号）→ 具体改法（≤3 条）→ 不改的部分。
3. 用户确认范围后开始逐段改写（见第 4 节规则）。

## 4. 改写规则（硬性，违反视为失败）

做：
- **只动句法，少动词汇**：调整句式、打断工整排比、变化句长节奏（混入 10–18 字短句与 40+ 字长句）、把抽象名词链具体化。
- **移动术语的句法角色**：术语本身不动，但避免总是放主语位置，可移到宾语/状语/定语或用指代复指重构。
- **削减功能重叠的连接词**：同段内同功能连接词只留 1 个，其余删去或改用句子逻辑承接。
- **拆除"模板段"**：拆工整排比为递进式长短句；删"值得注意的是/总的来说/不可忽视/具有重要意义"等套话及段尾元话语收束句。
- **密度差异化**：论证密度要有起伏；避免每段都是"背景铺垫→展开→收束"全等结构。
- **善用真实缓和信号**：有引用编号、实验数据、图表号、领域专名的段落置于显眼论证位置。
- **改写场景→按参考库第五节 11 条修复策略**：破三段式、去模板起笔、拆长句、注入具体性、变化节奏、去连接词/套话、破对称排比、去模糊归因、删填充短语、注入作者视角、泛化结论改可检验推论。
- **写新内容→按参考库第四节"预防性写作规则"8 条**：具体动词、长短句交替、不编号自然过渡、直接说事不铺垫、去连接词用标点、段长不均匀、用"而/则"替代"不仅…而且…"、穿插 ≤10 字短句（从源头避免，比事后修高效 5–10 倍）。
- **必避 AI 高频词**：此外、与此同时、综上所述、值得注意的是、具有显著优势、具有重要意义、不仅…而且…、其一…其二…其三…、深刻揭示、不可或缺、综合运用、有效解决、充分说明、进一步（滥用）。
- 每段改写后过**硬约束自检表**（参考库第六节），并说明"改了哪些高危特征、哪些没动、为什么"。

禁止：
- ❌ 换同义词（"显著"→"明显"）：检测器抓句法节奏，不抓词汇替换。
- ❌ 全面口语化或网络化：会从"AI 腔"倒向"博客腔"，导师观感更差。
- ❌ 删光"较为/一定程度上/往往/可能"等留余地的学术语——这是人写特征。
- ❌ 给术语加引号或"所谓……"制造距离感：这是 LLM 典型动作。
- ❌ 为降率篡改/编造数据、结论或引用；不得整体改变段落论证方向。
- ❌ 一次大改全部段落导致信息密度全线下降（新文本会因"空泛"被另类标记）。

## 5. 阶段 C：复查与迭代（循环直到达标）

改写后必须复查，形成闭环：

1. **硬约束自检（每段+全文，参考库第六节）**：AI 高频词每段 ≤2？段末套句全文 ≤1？三元并列每段 ≤1？"基于XX理论"起笔 ≤20% 段落？泛化结尾 =0？模糊归因 =0？加粗 ≤5 处？——全达标才算过。
2. **噪声预算（参考库第七节）**：是否保留了 2–3 处轻微 AI 特征、没有过度均质化？同时复核：事实数字未变？术语未动？无新增编造？仍是学术语体？
3. 对改写后文本**重跑信号扫描**：`python3 scripts/ai_signal.py <改后文件>`，与改写前对比。
4. **重新估算 AI 率**：本步同样**再开一个全新子 agent** 独立检测改写后文本（任务包写法与判定标准见第 2 节，不携带本会话历史与改写记录）；拿到新旧两份独立报告后并排对比，如实汇报新估算值与差距；未达标则列出剩余高危段落继续迭代（每轮只改当前最红的段落）。
5. 达标（估算 ≤ 目标）后输出总结：改动段数/总字符、命中特征消除情况、估算前后对比、自查结论，并提醒：**务必上知网等官方平台实测**；把标红段落回传后进入二次精修。

## 6. 交付形态

- 逐段修改意见与改写结果在对话内以"原文 → 改写文 → 改动说明"呈现；
- **精确保留导出 Word（关键）**：把改好的定稿原样装进 .docx，绝不重写内容：
  `python3 scripts/docx_write.py <定稿.txt> <输出.docx>`
  （数字/术语/公式/引号逐字保留，支持 `#` 标题与 `**加粗**`；导出后用 office_extract 回读可复核逐字一致。）
- **不要**用 walioffice 插件的 `doc_generate` 导这篇定稿——它会按主题**重写**成一篇新文章，
  数字、术语、你刚改好的表述全会被改掉。walioffice（doc/sheet/ppt_generate）只用于**从零生成**办公产物
  （如"根据主题生成一份 PPT/表格"）；已有正文/已定稿文本一律走 `docx_write.py` 精确保留导出。
- 导出成品文件（txt/md/docx）后告知路径；.pptx/.xlsx 的已定稿文本精确导出可按需扩展（本技能暂只做 docx 精确保留）。
